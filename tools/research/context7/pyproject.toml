[project]
name = "context7"
version = "0.1.0"
description = "Context7 library documentation search"
requires-python = ">=3.11"
dependencies = ["httpx"]

[tool.centaur]
module = "client.py"
hosts = ["mcp.context7.com"]
secrets = []
